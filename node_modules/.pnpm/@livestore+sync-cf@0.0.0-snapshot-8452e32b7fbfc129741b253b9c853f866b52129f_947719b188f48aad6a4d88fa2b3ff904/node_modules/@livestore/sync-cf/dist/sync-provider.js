import { SyncBackend } from '@livestore/common';
import { Effect } from '@livestore/utils/effect';
import { makeSyncBackend, makeSyncBackendFromUrl, inferTransportMode, } from "./sync-impl/transport-factory.js";
/**
 * Create a sync provider with explicit transport configuration
 */
export const createSyncProvider = (config) => {
    return makeSyncBackend(config);
};
/**
 * Create a sync provider from URL with automatic transport detection
 */
export const createSyncProviderFromUrl = (url, options = {}) => {
    return makeSyncBackendFromUrl(url, options);
};
/**
 * Utility to detect transport mode from URL
 */
export { inferTransportMode };
/**
 * Legacy exports for backward compatibility
 */
export { makeCfSync } from "./sync-impl/ws-impl.js";
export { makeHttpSync } from "./sync-impl/transport/http.js";
export { makeDoRpcSync } from "./sync-impl/transport/do-rpc.js";
//# sourceMappingURL=sync-provider.js.map